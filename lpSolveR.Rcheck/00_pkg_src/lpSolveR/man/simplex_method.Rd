% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex.R
\name{simplex_method}
\alias{simplex_method}
\title{Solve an LP Using the Simplex Method}
\usage{
simplex_method(
  obj,
  A_ub = NULL,
  b_ub = NULL,
  A_eq = NULL,
  b_eq = NULL,
  maximize = FALSE,
  tol = 1e-08,
  max_iter = 1000,
  bland = TRUE
)
}
\arguments{
\item{obj}{Numeric vector of objective coefficients (length n).}

\item{A_ub}{Numeric matrix of <= inequality constraint coefficients (m x n).
Default \code{NULL}.}

\item{b_ub}{Numeric vector of <= inequality RHS values (length m).
Default \code{NULL}.}

\item{A_eq}{Numeric matrix of equality constraint coefficients (p x n).
Default \code{NULL}.}

\item{b_eq}{Numeric vector of equality RHS values (length p).
Default \code{NULL}.}

\item{maximize}{Logical. Maximise the objective? Default \code{FALSE}.}

\item{tol}{Numeric tolerance for optimality and feasibility checks.
Default \code{1e-8}.}

\item{max_iter}{Maximum number of pivot iterations. Default \code{1000}.}

\item{bland}{Logical. Use Bland's rule (smallest index entering variable) to
prevent cycling? Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{solution}{Numeric vector of optimal variable values (original vars).}
\item{obj_value}{Optimal objective value (in original sense).}
\item{status}{One of \code{"optimal"}, \code{"infeasible"},
\code{"unbounded"}, \code{"max_iter_reached"}.}
\item{iterations}{Number of pivot steps performed.}
\item{runtime}{Elapsed time in seconds.}
\item{basis}{Integer vector of basic variable indices at termination.}
\item{tableau_history}{List of objective values after each pivot.}
}
}
\description{
Solves a linear program using the full tableau simplex method with optional
Bland's rule to prevent cycling. The problem is first converted to standard
form via \code{\link{lp_to_standard_form}}.
}
\examples{
obj  <- c(2, 3)
A_ub <- matrix(c(1, 1, 2, 1), nrow = 2, byrow = TRUE)
b_ub <- c(4, 6)
res  <- simplex_method(obj, A_ub, b_ub)
res$obj_value
res$solution

}
