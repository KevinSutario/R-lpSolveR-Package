% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interior_point.R
\name{interior_point}
\alias{interior_point}
\title{Solve an LP Using the Primal-Dual Interior Point Method}
\usage{
interior_point(
  obj,
  A_ub = NULL,
  b_ub = NULL,
  A_eq = NULL,
  b_eq = NULL,
  maximize = FALSE,
  mu = 10,
  tol = 1e-08,
  max_iter = 200
)
}
\arguments{
\item{obj}{Numeric vector of objective coefficients (length n).}

\item{A_ub}{Numeric matrix of <= inequality constraint coefficients (m x n).
Default \code{NULL}.}

\item{b_ub}{Numeric vector of <= inequality RHS values (length m).
Default \code{NULL}.}

\item{A_eq}{Numeric matrix of equality constraint coefficients (p x n).
Default \code{NULL}.}

\item{b_eq}{Numeric vector of equality RHS values (length p).
Default \code{NULL}.}

\item{maximize}{Logical. Maximise the objective? Default \code{FALSE}.}

\item{mu}{Initial barrier parameter. Default \code{10}.}

\item{tol}{Convergence tolerance for the duality gap. Default \code{1e-8}.}

\item{max_iter}{Maximum number of outer iterations. Default \code{200}.}
}
\value{
A named list:
\describe{
\item{solution}{Numeric vector of optimal variable values.}
\item{obj_value}{Optimal objective value.}
\item{status}{One of \code{"optimal"} or \code{"max_iter_reached"}.}
\item{iterations}{Number of iterations performed.}
\item{runtime}{Elapsed time in seconds.}
\item{duality_gap_history}{Numeric vector of duality gap at each iteration.}
}
}
\description{
Implements a log-barrier interior point method with simple barrier
parameter reduction to solve a linear program. The problem is first
converted to standard form.
}
\examples{
obj  <- c(2, 3)
A_ub <- matrix(c(1, 1, 2, 1), nrow = 2, byrow = TRUE)
b_ub <- c(4, 6)
res  <- interior_point(obj, A_ub, b_ub)
res$obj_value

}
