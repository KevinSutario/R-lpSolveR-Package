% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_phase_simplex.R
\name{two_phase_simplex}
\alias{two_phase_simplex}
\title{Solve an LP Using the Two-Phase Simplex Method}
\usage{
two_phase_simplex(
  obj,
  A_ub = NULL,
  b_ub = NULL,
  A_eq = NULL,
  b_eq = NULL,
  maximize = FALSE,
  tol = 1e-08,
  max_iter = 1000
)
}
\arguments{
\item{obj}{Numeric vector of objective coefficients (length n).}

\item{A_ub}{Numeric matrix of <= inequality constraint coefficients (m x n).
Default \code{NULL}.}

\item{b_ub}{Numeric vector of <= inequality RHS values (length m).
Default \code{NULL}.}

\item{A_eq}{Numeric matrix of equality constraint coefficients (p x n).
Default \code{NULL}.}

\item{b_eq}{Numeric vector of equality RHS values (length p).
Default \code{NULL}.}

\item{maximize}{Logical. Maximise the objective? Default \code{FALSE}.}

\item{tol}{Numeric tolerance. Default \code{1e-8}.}

\item{max_iter}{Maximum pivot iterations per phase. Default \code{1000}.}
}
\value{
A named list:
\describe{
\item{solution}{Numeric vector of optimal variable values.}
\item{obj_value}{Optimal objective value.}
\item{status}{One of \code{"optimal"}, \code{"infeasible"},
\code{"unbounded"}, \code{"max_iter_reached"}.}
\item{iterations}{Total pivot steps (both phases).}
\item{runtime}{Elapsed time in seconds.}
\item{basis}{Basic variable indices at termination.}
\item{tableau_history}{Objective values after each Phase 2 pivot.}
\item{phase1_iterations}{Pivot steps in Phase 1.}
\item{phase2_iterations}{Pivot steps in Phase 2.}
}
}
\description{
Implements the two-phase simplex method. Phase 1 minimises the sum of
artificial variables to find a basic feasible solution (BFS). Phase 2 then
optimises the original objective from that BFS.
}
\examples{
obj  <- c(1, 2)
A_eq <- matrix(c(1, 1), nrow = 1)
b_eq <- 3
res  <- two_phase_simplex(obj, A_eq = A_eq, b_eq = b_eq)
res$obj_value

}
